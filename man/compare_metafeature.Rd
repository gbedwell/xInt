% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_metafeature.R
\name{compare_metafeature}
\alias{compare_metafeature}
\title{Compare Meta-Feature Distributions}
\usage{
compare_metafeature(
  x,
  n.perm = 1000,
  sub.samp = 10000,
  seed = 1,
  stat = c("quantiles", "median", "mean"),
  quantiles = c(0.4, 0.5, 0.6),
  p.adj.method = "BH",
  bins = 100,
  combine.method = c("stouffer", "fisher")
)
}
\arguments{
\item{x}{The output of metafeature() with 'collapse = TRUE'.}

\item{n.perm}{The number of permutations. Defaults to 1000.}

\item{sub.samp}{The number of positions to sub-sample. Defaults to 10000.
Sub-sampling is ignored when NULL.}

\item{seed}{The random seed for reproducibility. Defaults to 1.}

\item{stat}{The test statistic to compare. One of 'quantiles' (default), 'median', or 'mean'.}

\item{quantiles}{The quantiles to calculate when stat = 'quantiles'. Defaults to c(0.4, 0.5, 0.6).}

\item{p.adj.method}{The method used to adjust p-values for multiple comparisons. Defaults to 'BH'.}

\item{bins}{The number of bins to use for binning the data. Defaults to 100. When NULL, operates on raw values.}

\item{combine.method}{Method for combining p-values when using multiple quantiles. One of 'stouffer' (default) or 'fisher'.}
}
\value{
A data frame holding the observed difference, p-value, and adjusted p-value for each comparison.
}
\description{
Compares meta-feature distributions between conditions.
Comparisons are made using permutation tests on the difference between mean or median values.
}
